function Get-FileFromWeb {
     [CmdletBinding()]
     param (
          [Parameter(Mandatory = $true, HelpMessage = 'The URL to the destination file.')]
          [string]$URL,

          [Parameter(Mandatory = $true, HelpMessage = 'The local file name to be stored as.')]
          [string]$LocalFileName
     )
     
     begin {
          #TODO: ADD IMPLEMENTATION
     }
     
     process {
          #TODO: ADD IMPLEMENTATION 
     }
     
     end {
          #TODO: ADD IMPLEMENTATION
     }
}